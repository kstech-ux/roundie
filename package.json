{
  "name": "roundie-lp",
  "version": "0.1.0",
  "private": true,
  "description": "ROUNDIE LP — static site, 3 locales, CSS-only motion",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:lh": "lighthouse-ci autorun",
    "test:a11y": "pa11y-ci && axe-cli http://localhost:5173/ja/",
    "test:vr": "playwright test"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "@lhci/cli": "^0.14.0",
    "pa11y-ci": "^3.1.0",
    "@axe-core/cli": "^4.10.0",
    "@playwright/test": "^1.45.0"
  }
}